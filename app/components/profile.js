// TODO: this is too big and the form elements should get broken down so there is not so much repeat elements
// TODO: seperate the information into contact information and user information
// TODO: include a add user button to submit information contact buy contact
// TODO: this can be where the new forms are brought together 
// TODO: I think this should become a functional conponent
// TODO: 0the forms are going to need to be class components because of the need to have controlled inputs

import React from 'react'
import UserInfo from "../containers/profile-user-info"
import ContactInfo from "../containers/profile-contact-info";

export const Profile = () => {

    <profile>
        <UserInfo/>
        <ContactInfo/>                       
    </profile>
       
}

